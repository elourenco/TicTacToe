import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 1,
  },
  board: {
    flex: 1,
    alignContent: "center",
    justifyContent: "center",
  }
});