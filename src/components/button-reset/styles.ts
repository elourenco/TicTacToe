import { StyleSheet } from "react-native";

export default StyleSheet.create({
  button: {
    backgroundColor: "red",
    alignItems: "center",
    justifyContent: "center",
    width: 'auto',
    height: 50,
    marginVertical: 30
  },
  text: {
    color: "white",
    fontSize: 20
  }
});